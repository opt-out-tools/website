import { ScrollerDirective } from './scroller.directive';

describe('ScrollerDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollerDirective();
    expect(directive).toBeTruthy();
  });
});
