import { FormStepDirective } from './form-step.directive';

describe('FormStepDirective', () => {
  it('should create an instance', () => {
    const directive = new FormStepDirective();
    expect(directive).toBeTruthy();
  });
});
